import { Routes } from '@angular/router';
import { ChartWithFilterComponent } from './chart-with-filter/chart-with-filter.component';

export const routes: Routes = [
    { path: 'chart', component: ChartWithFilterComponent }
];
